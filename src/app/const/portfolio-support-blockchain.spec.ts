import { PortfolioSupportBlockchain } from './portfolio-support-blockchain';

describe('PortfolioSupportBlockchain', () => {
  it('should create an instance', () => {
    expect(new PortfolioSupportBlockchain()).toBeTruthy();
  });
});
