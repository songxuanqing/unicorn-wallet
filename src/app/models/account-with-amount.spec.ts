import { AccountWithAmount } from './account-with-amount';

describe('AccountWithAmount', () => {
  it('should create an instance', () => {
    expect(new AccountWithAmount()).toBeTruthy();
  });
});
